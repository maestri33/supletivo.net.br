---
import { ModernHeroSection, type ModernHeroSectionProps } from './modern-hero';

interface Props extends ModernHeroSectionProps {
  client?: 'load' | 'idle' | 'visible';
}

const props = Astro.props;
---

<ModernHeroSection client:load {...props} />
